# Matths 웹·iPad 코드 인수인계

> 작성 기준: 2026-08-13 KST
>
> 공개 통합 저장소 작업 브랜치: `sync/canonical-d6c6293-8869906`
> 동기화 직전 공개 `main`: `5575b8b50043006bb9c2b4a6f7b8f40bbbb8a336`
> 운영 기준 origin: `https://www.matths.kr`

## 1. 반드시 지킬 경계

GOAT Arena의 매칭, 난이도, 문제 구성, 티어, 공격·방어 횟수, 예치, 반환·이전·소각,
페이백, Ranked 학습일수, 상점 가격·자격·효과·원장과 경기 정산 규칙은 동결 상태다.
버그처럼 보여도 임의 수정하지 말고 코드·화면·문서의 불일치를 근거와 함께 보고한 뒤
제품 소유자의 별도 승인을 받아야 한다.

웹 이력의 `4e568cf`는 Arena 설명 문서를 런타임 값과 맞추려 했던 커밋이며,
사용자 지시 뒤 `674b494`로 전체 revert했다. 따라서 최종 스냅샷에서 Arena 규칙 문서와
런타임의 순변경은 0이다. 두 커밋 중 하나만 골라 적용하면 안 된다.

운영 DB migration·index 삭제, 결제 운영 활성화, Cafe24 배포, Google Console 변경,
서명키·프로비저닝 변경도 별도 운영 승인 없이 실행하지 않는다.

## 2. 기준 원본

| 영역 | 원본 경로 | 고정 SHA |
|---|---|---|
| 웹 | `matths-working/webrepo-applied` | `d6c62938fdd3726e0f7d052b47b57142eb42c68a` |
| iPad | `matths-working/ipad-app` | `8869906fbb9a376b7015820a824506a79b7d2989` |
| 원본 upstream | `is4553807/Matths` `main` | `023a473` |
| 공개 저장소 rollback | `rollback/pre-curriculum-sync-20260813-5575b8b` | `5575b8b` |

`web/`과 `ipad/`는 위 원본의 tracked content snapshot이다. 원본 Git commit 객체와
전체 이력은 이 통합 저장소에 포함되지 않는다. 웹의 과거 검수 스크린샷, PDF, `tmp`,
`evidence`, `dist`, `node_modules`와 iPad KICE PDF, `llama.xcframework`, 빌드·서명 산출물은
의도적으로 제외했다.

## 3. 이번 스냅샷의 핵심 기능

### 3.1 커리큘럼 스토리와 내레이션

- 2022 교육과정 기준 13과목 220개 개념, 1,100개 장면을 웹과 iPad에 동일한 byte로 싣는다.
- 각 개념은 직관, 질문, 오개념, 풀이, 회상 장면을 포함하고 240~360초 학습을 목표로 한다.
- 학생 화면 자막과 음성용 원고를 분리하며 스튜디오 지시 태그는 학생 데이터에서 거부한다.
- 기본 재생은 `ko-KR` 여성 시스템 TTS이고, 편집 manifest는 ElevenLabs v3용으로 별도
  export한다. ElevenLabs 키나 합성 음원은 저장소에 포함하지 않는다.
- 웹과 iPad 모두 account/revision별 체크포인트, 중단 복구, watchdog, stale callback
  무효화 및 콘텐츠 완전성 release gate를 갖는다.

### 3.2 iPad 사용자 흐름

- 오답 분석 중 사용자가 방금 제출한 손글씨 풀이를 플로팅 카드로 계속 볼 수 있다.
- 9개 티어 승급 효과는 1080×1920, 60fps, 6초 H.264 MP4를 `AVPlayer`로 재생한다.
  원본 비율을 유지하고 44pt 건너뛰기 버튼과 Reduced Motion 최종 프레임을 제공한다.
- 이 변경은 승급 연출 렌더러만 바꾸며 Arena 티어 판정·MMR·경기 정산에는 관여하지 않는다.

### 3.3 운영 URL과 Google OAuth

- 웹·iPad 운영 origin은 모두 `https://www.matths.kr`이다.
- Google 웹 callback은 `https://www.matths.kr/auth/google/callback`을 사용한다.
- client id/secret, DB URI와 토큰 서명키는 공개 소스가 아니라 Cafe24 환경변수에만 둔다.
- 코드와 OAuth 자격증명 검증은 끝났지만 Cafe24에 최신 후보를 올리고 프로세스를 재시작하는
  작업은 호스팅 접속 정보가 없어 별도 운영 작업으로 남아 있다.

## 4. 웹 로직·인프라 변경

### 4.1 Google 로그인 표시와 운영 경계

- `views/login.ejs`: 서버가 Google OAuth 준비 완료를 반환할 때만 Google 로그인 블록을
  렌더한다. 미설정 상태의 비활성 `설정 중` CTA는 제거했다.
- OAuth·PKCE·토큰 교환 로직은 기존 구현을 유지한다.
- `config.env`는 access token이 아니라 서버용 OAuth client id/secret/redirect URI다.
  앱이나 Git에 넣지 말고 Cafe24 환경변수로만 주입한다.
- stale Cafe24 배포에서는 로그인 전 Google 시작 라우트가 Bearer 인증 뒤에 있어 401이
  났다. 현재 후보의 `/auth/google/app` 및 legacy `/api/v1/auth/google/start`는 공개
  경계에 있다. 환경변수만 바꾸고 구 코드를 유지하면 해결되지 않는다.

### 4.2 학생 통신 실패 복구

- 신규 `public/js/fetch-error-message.js`가 네트워크 단절, timeout, 잘못된 JSON과
  안전한 서버 한국어 메시지를 구분한다.
- 적용: concept experience, main, private mock, quick practice, study hall, unit learning,
  wrong-note review 및 Arena 경기의 표시 오류 경계.
- 실패 뒤 버튼·입력·토글을 복구하며 `error.message` 영문을 학생에게 직접 노출하지 않는다.
- Arena에서는 오류 표현과 접근성 alert만 바뀌었고 경기 상태·채점 규칙은 바뀌지 않았다.

### 4.3 MathJax 자체 호스팅

- `mathjax@4.1.3`, `views/partials/mathjax-runtime.ejs`, `/vendor/mathjax` 정적 경로 추가.
- 평가, 퀵 연습, 단원 학습, 오답, 사설 모의, Arena 경기·분석 및 관리자 수식 화면이
  외부 CDN 대신 같은 서버의 고정 버전을 사용한다.
- 문제·정답·수식 내용은 바꾸지 않았다.

### 4.4 iPad 원본 탐색과 출시 게이트

- `scripts/resolveIpadWorkspace.js`: `MATTHS_IPAD_ROOT` → `../ipad-app` → `../ipad` 순으로
  탐색하고 통합 저장소에서는 `SOURCE-SNAPSHOT.json` provenance를 확인한다.
- Cafe24 패키징 전에 전체 테스트를 강제한다.
- 승인 캡처는 clean tree, 정확한 source SHA, CDP, HTTP 상태, 인증, viewport,
  full-page 이미지와 document/intrinsic overflow를 모두 통과해야 한다.
- 예상 오류 fixture도 지정 상태 코드와 카피가 맞아야 통과하며 production에서 fixture
  서버가 실행되지 않도록 차단했다.

### 4.5 결제·상점 표시 경계

- `views/pricing.ejs`: 결제가 열린 환경에서 `과금 없음/준비 중` 문구가 남지 않도록 조건화.
- `views/payment-result.ejs`: 공통 결제 CSS를 불러 모바일 320/390 overflow를 해소.
- `parent-v2.css`: 결제 결과의 주·보조 행동을 48px 버튼으로 분리하고 520px 이하에서
  한 열로 배치해 링크가 붙어 보이던 문제를 해소.
- `services/arenaShopPolicyService.js`: iPad가 raw 경기 ID를 입력하지 않도록 분석·보호권
  선택 대상의 표시 배열을 API에 추가. 가격, 차감, 원장, 상품 자격과 경기 정산은 변경 없음.

### 4.6 빈 상태와 복구

- 랜딩: 랭킹 데이터가 없을 때 영구 `갱신 중` 대신 명확한 시즌 준비 상태.
- 학부모: 풀이 표본 0건일 때 `오답률 100%` 대신 `—`와 빈 상태.
- 사설 모의: 잠긴 사용자에게 일정·구성·절차 미리보기, 시험 시작 실패는 별도 alert,
  버튼과 저장 상태 복구.

## 5. EJS 변경 지도

| 파일군 | 변경 목적 |
|---|---|
| `views/login.ejs` | Google 준비 완료 시에만 로그인 블록 노출 |
| `views/pricing.ejs`, `payment-result.ejs` | 결제 상태 조건·모바일 스타일 |
| `views/index.ejs`, `parent-dashboard.ejs` | 랭킹·학부모 무표본 빈 상태 |
| `views/private-mock-exams.ejs`, `private-mock-exam.ejs` | 잠금 미리보기·실패 alert |
| `views/partials/mathjax-runtime.ejs` | 자체 호스팅 MathJax 공통 로더 |
| 평가·학습·오답·관리자 수식 EJS | MathJax partial과 한국어 오류 mapper 연결 |
| `views/goat-arena-main-battle.ejs` | 기존 자격 상태에서 첫 행동 위치를 찾기 쉽게 재배치; 규칙 불변 |
| `views/goat-arena-main-shop-analysis.ejs` | 유료 분석 결과 TeX 렌더링 |
| `views/partials/admin-navigation.ejs` | 좁은 폭 관리자 identity |

## 6. CSS 변경 지도

- `admin.css`, `admin-v2.css`: 밝은 헤더 대비, active nav, 모바일/태블릿 필터,
  위험 행동을 outline danger로 강등.
- `assessment.css`, `community.css`, `concept-experience.css`, `unit-learning.css`:
  학생 장문 본문을 16px와 읽기 가능한 line-height로 조정. 메타·라벨의 12px은 유지.
- `legal.css`: 개인정보 표 카드 전환 breakpoint를 1100px로 확대.
- `index.css`: 901–1050px 랜딩 헤드라인 충돌 해소.
- `private-mock-exams.css`: 잠금 미리보기와 모바일 1열.
- `product-refresh.css`: 밝은 표면에 남아 있던 흰색/저대비 전경색 복구.
- `store.css`: 761–900px 수험관 내비 소실 방지.
- `parent-v2.css`: 좁은 화면 hero 정렬.
- `goat-arena.css`: 중복 카드, metrics 배치와 모바일 CTA 레이아웃만 정리.
  Arena 수치·경제·규칙 의미를 바꾸는 CSS/EJS 수정은 금지한다.
- 실제 포함 서체가 지원하지 않는 weight 950은 900으로 정규화하고 회귀 테스트를 추가했다.

## 7. iPad 변경

### 7.1 화면 보호

- `ScreenshotGuard.swift`의 공통 `ScreenProtectionLayer`를 앱 루트와 Arena
  `fullScreenCover`가 함께 사용한다.
- 화면 녹화·미러링과 background 전환 cover, 스크린샷 사후 감지, 가명 워터마크,
  무결성 이벤트를 유지한다.
- iOS에는 일반 정지 스크린샷을 촬영 순간 앱이 완전히 차단하는 공식 API가 없다.

### 7.2 퀵 연습과 상점 선택

- 평가센터에 기존 `.quickPractice` 네이티브 진입점을 연결했다.
- Arena 상점의 raw match ObjectId 입력을 서버 제공 Picker로 교체했다.
- 구 서버 응답과 하위 호환하며 가격·구매 원장·효과·정산은 변경하지 않았다.

### 7.3 브랜드·Release 검사

- Splash의 typed `Matths` 재조합을 없애고 공식 symbol asset만 사용한다.
- Release 바이너리 검사는 macOS `strings` 대신 UTF-8 binary-safe scanner와
  positive control을 사용한다.

### 7.4 Ling 3.0 tiny

- MIT 공개·비게이트 후보임을 확인했지만 Release/UI에는 연결하지 않았다.
- text-only라 Qwen 비전 판독을 대체하지 못하고, 현재 llama.cpp에는 필요한
  `bailingmoe3`가 없어 직접 로드할 수 없다.
- DEBUG 후보 gate만 있으며 upstream runtime 정식 지원 뒤 M2 8GB에서 Q3_K_M부터
  jetsam·속도·수학 정확도 실기 검증이 필요하다.

## 8. 검증 명령과 현재 결과

- canonical 웹과 공개 통합 checkout: 138/138 전체 회귀 통과, EJS 120개와 추적
  JS/MJS 685개 정적 검사 통과.
- canonical iPad: 57/57 계약 통과, 220/220 스토리와 15/15 번들 검사 통과.
- 서명된 iPad 실기 빌드·설치·실행 통과, 9개 rank 영상 재생 증거 9/9 통과.
- 내부 전달 IPA `Matths-internal-debug-8869906.ipa`의 SHA-256은
  `9bab3ec428fa64253322c951c7d5ab920ce8c4a52ae12091c022b744a1bbdf30`이다. IPA 자체는
  공개 저장소에서 제외한다.
- 공개 checkout: `ipad/`에서 웹 경로를 `MATTHS_WEB_REPO=../web`로 지정하면 55/57 iPad 계약 통과.
  나머지 둘은 의도적으로 제외한 `llama.xcframework`와 KICE PDF를 요구하는 내부 검사다.

웹 통합 저장소에서:

```sh
cd web
npm test
npm run ui:verify
node scripts/run-tests.js --check
git diff --check
```

iPad:

```sh
cd ipad
for script in tests/run-*.sh; do bash "$script"; done
xcodebuild -project Matths.xcodeproj -scheme Matths -configuration Debug \
  -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO build
```

공개 clone에는 재배포하지 않는 `Frameworks/llama.xcframework`와 사용 허락 전 KICE PDF
9개가 없다. `MATTHS_WEB_REPO=../web`로 통합 웹 경로를 지정하면 외부 리소스 없이 55/57이
통과하고, Ling runtime 검사와 KICE resource 검사 두 개만 의도적으로 실패한다. 검증된 내부
작업본에서는 57/57과 서명된 기기 빌드·설치·실행이 통과했다. KICE PDF를 공개 저장소에
추가해 계약 수를 맞추는 방식은 금지하며, llama framework도 라이선스·출처·해시를 확인한
배포 경로를 먼저 마련해야 한다.

## 9. 운영에서 남은 일

1. Cafe24에 후보 웹을 배포하고 프로세스를 재시작한다.
2. Google client id/secret/redirect, `PUBLIC_BASE_URL`, 기존 운영 `API_TOKEN_SECRET`과
   production preflight 필수 secret을 서버 환경변수로 설정한다.
3. Google Console에 `https://www.matths.kr/auth/google/callback`을 정확히 등록한다.
4. `matths.kr` TLS SAN·vhost를 바로잡고 `/api/v1/auth/providers`가 200인지 확인한다.
5. 만료 임박한 iOS provisioning을 갱신하고 두 번째 기기 또는 TestFlight 경로를 준비한다.
6. Atlas stale index 도구는 dry-run만 완료했다. 백업·승인 전 apply 금지.

## 10. 롤백

- 원격 branch: `rollback/pre-curriculum-sync-20260813-5575b8b`
- annotated tag: `snapshot/pre-curriculum-sync-20260813-5575b8b`
- 보존 commit: `5575b8b50043006bb9c2b4a6f7b8f40bbbb8a336`
- 새 sync 브랜치에서 문제가 생기면 `main`을 강제 덮지 말고 PR을 닫은 뒤 위 rollback
  ref의 파일을 기준으로 복구한다.

## 11. v8 재심사 후속 상태

수신된 단일 `report-main.html`은 fragment placeholder가 남은 불완전본이었다. 판단에는
무결성 해시가 일치한 v8 ZIP 안의 `matths-reaudit-v8-report.html`과 ledger JSON을 사용했다.
아래는 다음 작업자가 중복 수정하지 않기 위한 현행 상태다.

| 보고서 축 | 상태 | 현재 처리 |
|---|---|---|
| S028 | 코드 완료 | iPad 경로를 공통 resolver로 통일 |
| S029~S031 | 코드 완료 | 패키징 전 테스트와 캡처 manifest·commit·overflow fail-closed |
| S043 | 코드 완료 | 결제 준비 문구를 실제 결제 상태에 맞춰 조건화 |
| S044~S045 | 코드 완료 | 학생 fetch 오류 한국어화와 CTA·입력 복구 |
| S055, S057 | 코드 완료 | 핵심 안내·장문 본문 대비와 16px 읽기 크기 |
| S056 | 코드 완료 | MathJax 4.1.3 자체 호스팅 |
| S071 | iPad 코드 완료 | 전체화면 Arena 경기에도 공통 보호 레이어 적용 |
| S086 | iPad 코드 완료 | Splash typed wordmark 제거, 공식 asset 사용 |
| S103 | iPad 코드 완료 | UTF-8 binary-safe Release marker 검사 |
| S118 | 코드 완료 | Arena 분석 결과 TeX 렌더링 |
| S119 | 코드 완료 | 내부 match id 수동 입력을 서버 선택 Picker로 교체 |
| S141 | iPad 코드 완료 | 퀵 연습 진입점 연결 |
| S142~S143 | 증거 완료 | 결제·오류 18면 × 5폭 = 90캡처, viewport/full-page 180 PNG, 실패·overflow 0 |
| S104 | 외부 운영 필요 | provisioning 갱신, 추가 기기 또는 TestFlight |
| C149 | 코드 완료·운영 필요 | 미설정 dead CTA 제거; Cafe24/Google Console 설정은 별도 |
| S117/C215 및 Arena 규칙 지적 | 동결 | 법무·제품 결정 전 코드·문서·수치 변경 금지 |

보고서의 `matths.kr DNS 미해결`은 현재 시점에는 낡은 관측이다. DNS는 해석되지만
apex TLS/vhost와 배포본 API 경계가 잘못되어 있으므로 운영 수정 후 다시 검증해야 한다.

S142~S143 증거는 canonical web `0734ce0`의 clean tree와 CDP로 생성했다. 공개 Git에는
PNG를 넣지 않았으며 로컬 manifest SHA-256은
`bf3a5b71da89e607490b91bd450c4f28e50fe7084affe53e36a968833c2df771`이다.
