# Matths 웹·iPhone·iPad 인수인계

> 기준: 2026-08-15 KST
>
> 공개 sync 브랜치: `sync/canonical-1c2fe6c-63d7f5e-20260815`
>
> 운영 정본 origin: `https://www.matths.kr`

## 1. 고정 소스와 절대 경계

| 영역 | canonical commit | 검증 |
|---|---|---|
| 웹 | `1c2fe6ccad89557518dfc968036906ca7a11a045` | 148/148, EJS 120, JS/MJS 722 |
| iPhone+iPad | `63d7f5e5a5121ebe9d79cc424885f4b2c1cae81f` | shell contract 62/62, Debug Simulator build; iOS 17+ scene capture 정식 신호 적용 |

GOAT Arena의 매칭, 난이도, 문제 구성, 티어 정의, 공격·방어 횟수, 예치, 반환·이전·소각,
페이백, 상점 가격·자격·효과·원장과 경기 정산은 동결이다. 이번 기준은 직전 공개 원본
`d6c62938`/`8869906` 이후 Arena 제품 경로 순변경이 0임을 확인했다. 원장에 남은 Arena
감사 항목은 모두 `report-only`, `frozen`이며 구현 지시가 아니다.

## 2. Google 로그인과 운영 URL

- 웹·앱 운영 origin은 `https://www.matths.kr` 하나다.
- callback은 `https://www.matths.kr/auth/google/callback`이다.
- iPhone/iPad는 `ASWebAuthenticationSession`의 플로팅 시스템 브라우저에서
  `/auth/google/app?code_challenge=...`를 연다. 이는 정상 네이티브 로그인 UX다.
- 앱 bridge grant는 RFC 7636 형식 verifier, S256 challenge, AES-256-GCM으로 저장한
  정확한 최초 응답, 원자 CAS와 60초 retry window를 사용한다.
- 같은 code+verifier의 순차·20-way 동시 요청은 byte-identical 응답으로 수렴한다.
  사용자 차단과 `tokenVersion` 변경은 재생보다 우선한다.
- 구형 `/api/v1/auth/google/start` 공개 별칭은 제거했다.
- `config.env`는 access token이 아니라 OAuth client id/secret/callback 등 서버 자격증명이다.
  앱·Git·배포 아카이브에 넣지 않는다.

로컬 최신 후보에서는 providers 200/configured, modern start 302, callback exact www,
release fingerprint 일치를 확인했다. 2026-08-15 13:01 KST 현재 운영 `www.matths.kr`는
providers 401, modern app route 404로 구버전이다. DNS상 `www`는 Cloudtype,
apex `matths.kr`만 Cafe24를 가리킨다. 따라서 현재 Cloudtype에 배포하거나 `www` DNS·TLS·
vhost를 Cafe24 최신 앱으로 전환하기 전에는 실앱 Google 로그인이 완료되지 않는다.

## 3. 완료된 사용자 기능

### 커리큘럼

- 13과목 220개 개념, 1,100개 장면, 개념당 직관→질문→오개념→풀이→회상 5단계다.
- 음성용 narration과 화면 subtitle은 분리한다.
- ElevenLabs v3용 studio script에는 감정 태그를 보존하고 학생 projection에서는 태그를 거부한다.
- 웹·앱 기본 음성은 `ko-KR` 여성 시스템 TTS 우선이며 provider 교체 경계를 갖는다.
- Apple TTS는 문장 chunk별 실제 Yuna 합성으로 220개 전수 측정했다. 최단 234.900초,
  최장 346.678초, 평균 274.637초, timeout 0이다. 재현기는
  `ipad/tests/manual/run-curriculum-narration-duration-audit.sh`다.
- 웹과 Apple의 rate 스케일은 달라 웹은 `0.68`, Apple은 `0.55`를 사용한다.
- 중단 checkpoint, stale callback 무효화, watchdog, background 중단·재개가 있다.
- 커리큘럼 상단에는 기존 `continueConcept` 하나만 사용한 5-node compact 기억선이 있다.
  새 해금·진도 규칙은 만들지 않았다.
- release gate는 story/voice를 각각 정확히 220/220으로 강제하고 웹↔앱 15파일 byte parity를 확인한다.
- 220개념·1,100장면 각각에 `focus → connect → verify` 모션 3동작을 직접 기록했다.
  제목을 바꿔 끼우는 공용 fallback은 0개다.
- 현재 대상은 사각 강조 레이어·밑줄·펜 포인터로 표시하며, 한 화면에는 현재 장면 하나와
  현재 질문 하나만 우선 노출한다.
- `이해했어요`는 3지 확인 문제로 이어지고, 오답은 근거 피드백 뒤 순한맛 재설명으로
  분기한다. 같은 장면 두 번 오답이면 자동으로 순한맛을 연다. 매운맛은 조건·기호·함정을
  압축해 보여준다.

### 맵쓰 코치

- 코치 문구는 무작위 감탄사가 아니라 `관찰 → 이유 → 다음 행동` 세 줄 진단이다.
- 확률·통계·지수·로그·기하·미적분·수열 등 문제 유형에 따라 다음 행동을 바꾼다.
- 코치는 채점·숙련도 판정 권한이 없고, 답을 바꾸지 않는다.

### 화면 밀도와 보호

- 학습 화면은 좌상단에서 우하단으로 `단원 → 개념 → 목표 → 선생님 보드 → 현재 질문 → 행동`
  순서를 유지한다. 장문 5개 동시 노출과 반복 대각 워터마크는 제거했다.
- 문제 화면 워터마크는 우하단 한 곳, opacity 3.5%로 제한한다.
- 화면 녹화·AirPlay·미러링은 iOS 17+ 정식 `isSceneCaptured` 신호로 즉시 보호 덮개를
  띄우며, deprecated `UIScreen.isCaptured`는 초기화 폴백으로만 유지한다.
  앱 전환기에도 내용을 가린다.
- 일반 SwiftUI/UIKit 화면의 일회성 스크린샷 결과를 Netflix 영상처럼 강제로 검정으로 만드는
  공개 iOS API는 없다. Netflix의 검정 캡처는 FairPlay 영상 보호 경로다. 저장 뒤 도착하는
  screenshot notification이나 비공개 `secureTextEntry` 우회로 완료를 꾸미지 않는다.

### 오답 풀이 플로팅

- iPad와 웹 모두 오답 분석 중 방금 제출한 손필기 풀이를 플로팅으로 유지한다.
- 웹은 mouse/touch/Pencil canvas를 지원하고 오답+비어 있지 않은 필기에만 로컬 PNG Blob을 만든다.
- 필기 이미지는 서버에 업로드하지 않고 Object URL을 정리한다.
- 분석 provider는 `local`, `memory-only`, `network:false` capability일 때만 호출한다.
- 채점·mastery와 분석 표시는 분리돼 있다.

### 휘장

- 9개 원본 MP4를 `AVPlayer` 하드웨어 디코딩 경로로 재생한다.
- 효과를 단순 SwiftUI 연출로 대체하지 않는다.
- 건너뛰기 44pt, 영상 종료 자동 닫기, Reduce Motion 최종 프레임 무음 표시가 있다.
- 물리 iPad 후보 2회에서 9/9 성능 기준을 통과했다.
- 최종 빌드는 commit/tree, 실행파일, asset manifest, 9개 MP4 SHA를 묶는다.
- MP4 외부 원본 승인 provenance는 아직 `unknown`이며 external attestation 전
  `appStoreEligible=false`다.

### 유니버설 앱

- Xcode target device family는 iPhone+iPad(`1,2`)다.
- iPhone compact chrome, safe-area, landscape icon tab, 44pt target, Dynamic Type를 지원한다.
- iPhone은 손가락 필기 기본, iPad는 Pencil-only 기본과 기존 손가락 토글을 유지한다.
- iPhone/iPad simulator build와 실제 iPad 설치·launch를 통과했다.
- 실제 iPhone notch·키보드·카메라·Google 플로팅 로그인은 실기 기기 검증이 남아 있다.

## 4. 감사 원장 정정

`web/docs/principles-smell-audit.json`은 작업 목록이 아니라 historical ledger다.

- 원시 역사 항목: 121(P1 29/P2 88/INFO 4)
- 현재 사실 항목: 118(P1 29/P2 86/INFO 3)
- 반증된 PencilKit 다크모드 소실 `0274`: `retracted`
- iPhone 미지원 `0132`, `1706`: `resolved` by `bcf0234`
- 운영 보안헤더 “전무”: 철회하고 config-as-code drift P2로 분리
- Arena 관련 40건: `report-only + frozen`, `fixDirection` 없음
- 원문 PDF와 12-agent raw journal이 저장소에 없어 121건 방법론 완전 커버는 독립 검증 불가

## 5. 검증 결과

웹 canonical:

```sh
cd web
npm test                       # 148/148
npm run ui:verify              # 120 EJS
node scripts/run-tests.js --check
npm run curriculum:story:release-gate
npm run curriculum:voice:release-gate
node scripts/syncIpadCurriculumStories.js --check
git diff --check
```

iPhone+iPad canonical:

```sh
cd ipad
for script in tests/run-*.sh; do bash "$script"; done  # 62/62
xcodebuild -project Matths.xcodeproj -scheme Matths \
  -configuration Debug -destination 'generic/platform=iOS Simulator' \
  CODE_SIGNING_ALLOWED=NO build
```

직전 `4a77c22`에서 서명·설치한 iPad 실행파일 SHA-256은
`24d04a902ba70120cad7e20530b623dcfb69f070c1931338c50f8791447e254b`다.
동일 앱의 codesign·rank bundle provenance 검증과 설치까지 통과했다. 이후 `63d7f5e`에서
scene capture 정식 감지 경로를 추가해 62/62와 Debug Simulator build를 통과했지만 아직
재서명·재설치하지 않았다. 자동 launch도 기기가 잠겨 차단됐으므로 최신 binary의 실기 실행·
Google 왕복 완료를 주장하지 않는다.
서명 identity·Team ID·UDID·IPA는 공개 저장소와 외부 검수 패키지에 포함하지 않는다.

공개 통합 checkout에서도 웹 148/148가 통과했다. iPad는 60/62이며 실패한 둘은
의도적으로 제외한 `llama.xcframework`를 검사하는 Ling runtime 계약과 KICE PDF 9개를
검사하는 resource 계약이다. 나머지 예상 밖 실패는 0이다.

## 6. 공개 snapshot 제외

- 웹 `docs/web-redesign/screenshots/**` 69개, `tmp/pdfs/**` 5개
- `evidence`, `dist`, `node_modules`, local DB와 브라우저 profile
- iPad KICE PDF 원본, `llama.xcframework`
- DerivedData, build, IPA/XCArchive, 인증서·프로비저닝·환경 비밀

이 제외는 제품 소스 누락이 아니라 개인정보·저작권·생성 증거·빌드 산출물 경계다.

## 7. 남은 외부 작업

1. `www`를 현재 Cloudtype에 유지할지 Cafe24로 옮길지 결정하고, 선택한 실제 `www` 호스트에
   웹 `1c2fe6c`를 배포해 Node 프로세스를 재시작한다.
2. 선택한 실제 `www` 서버에 Google client id/secret, exact www callback, `PUBLIC_BASE_URL`, 기존 운영
   `API_TOKEN_SECRET`과 production 필수 secret을 설정한다.
3. Google Console authorized redirect에 exact www callback을 등록한다.
4. 배포 후 health fingerprint, providers 200, modern start 302, exchange, 신규·기존 계정,
   취소·재시도·탈퇴 재인증을 실제 왕복 검증한다.
5. 실제 iPhone에서 회전·키보드·카메라·Google 플로팅 로그인과 손가락 필기를 검증한다.
6. rank MP4 원본 승인 provenance, LICENSE/NOTICE, KICE 파생 데이터 권리를 확정한다.
7. 실제 iOS 여성 음색·VoiceOver 완주와 ElevenLabs v3 합성 교체를 실기 검수한다.

## 8. 롤백

- branch: `rollback/pre-motion-sync-20260815-717490f`
- annotated tag: `snapshot/pre-motion-sync-20260815-717490f`
- commit: `717490fdef70586d5cd7bdb78e7875cc4590253c`

문제가 생기면 main을 force-push하지 말고 sync PR을 닫은 뒤 위 ref에서 복구한다.
